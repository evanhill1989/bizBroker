export default function SellersOnboarding() {
  return (
    <div>
      <h1>
        This page is the fallback for users that somehow got into onboarding
        flow without clicking dialog link(NewListingDialog())
      </h1>
    </div>
  );
}
